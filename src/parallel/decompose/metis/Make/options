EXE_INC = \
    -I$(METIS_ARCH_PATH)/include \
    $(if $(METIS_INCLUDE_DIR),-I$(METIS_INCLUDE_DIR)) \
    -I../decompositionMethods/lnInclude

LIB_LIBS = \
    -L$(METIS_ARCH_PATH)/lib \
    $(if $(METIS_LIB_DIR),-L$(METIS_LIB_DIR)) \
    -lmetis

ifeq ($(WM_ARCH),mingw64)
LIB_LIBS += -ldecompositionMethods -lmsmpi
endif
